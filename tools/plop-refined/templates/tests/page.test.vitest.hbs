import { describe,it,expect } from "vitest";import { render } from "@testing-library/react";import Page from "../{{pascalCase name}}.page"; describe("{{pascalCase name}} page",()=>{ it("renders",()=>{ const {getByText}=render(<Page/>); expect(getByText(/{{pascalCase name}} page/)).toBeTruthy(); }); });