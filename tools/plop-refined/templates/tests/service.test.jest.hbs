import { create{{pascalCase name}}Service } from "../{{kebabCase name}}.service"; test("sets",()=>{ const s=create{{pascalCase name}}Service(); s.setState?.(42); expect(s.getState?.()).toBe(42); }); }