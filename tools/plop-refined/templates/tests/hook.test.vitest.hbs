import { describe,it,expect } from "vitest";import { renderHook,act } from "@testing-library/react";import { use{{pascalCase name}} } from "../use{{pascalCase name}}.hook"; describe("use{{pascalCase name}}",()=>{ it("updates",()=>{ const {result}=renderHook(()=>use{{pascalCase name}}()); act(()=>result.current.setState(42)); expect(result.current.state).toBe(42); }); });