import { describe,it,expect } from "vitest";import { create{{pascalCase name}}Service } from "../{{kebabCase name}}.service"; describe("{{pascalCase name}} service",()=>{ it("sets",()=>{ const s=create{{pascalCase name}}Service(); s.setState?.(42); expect(s.getState?.()).toBe(42); }); });