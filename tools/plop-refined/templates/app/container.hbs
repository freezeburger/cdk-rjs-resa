import React from "react"; import { useSelector,useDispatch } from "react-redux"; import { use{{pascalCase name}}Logic } from "./use{{pascalCase name}}Logic"; export default function {{pascalCase name}}Container(){ const d=useDispatch(); const s=useSelector((x:any)=>x); const logic=use{{pascalCase name}}Logic(); return <section><h2>{{pascalCase name}} Container</h2><pre>{JSON.stringify(s,null,2)}</pre><button onClick={()=>d({type:"{{kebabCase name}}/ping"})}>Dispatch ping</button><div>{JSON.stringify(logic)}</div></section> }