import { useMemo,useState } from "react"; export function use{{pascalCase name}}Logic(){ const [count,setCount]=useState(0); const inc=()=>setCount(c=>c+1); return useMemo(()=>({count,inc}),[count]) }