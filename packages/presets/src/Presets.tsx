import { tokens } from '@cdk/tokens'

export const neutral = {
  brand: { accent: tokens.color.accent[500] },
  radius: 'md' as const
}

export const ocean = {
  brand: { accent: '#0ea5e9' },
  radius: 'lg' as const
}
